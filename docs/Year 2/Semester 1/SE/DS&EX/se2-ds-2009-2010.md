---
sidebar_position: 8
title: "Devoir Surveillé — Systèmes d'exploitation (17/11/2009)"
sidebar_label: DS 2009/2010
hide_title: true
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
<TabItem value="markdown" label="Markdown" default>

# Devoir Surveillé — Systèmes d'exploitation

*École Nationale des Sciences de l'Informatique — Classes : II2 — Module : Systèmes d'exploitation — A.U. : 2009/2010 — Date : 17/11/2009 — Durée : 2h00 — Documents non autorisés — Enseignants : F. Najjar, M. S. Ouerghi, B. Belgacem && A. Chabchoub*

<!-- TODO: unclear in source, verify against original PDF — this source PDF (ds09-10.pdf) is a scan with weak OCR quality; no separate correction document was provided for this DS (task list gave a single fileId with no "corr" counterpart). Several figures/diagrams (a process-state transition diagram in Exercice 1.2, a precedence graph in Exercice 3, and parts of the Exercice 2 C code) did not extract cleanly as text — flagged individually below. This is a statement-only transcription; no correction is available. -->

## Exercice 1 : Questions de cours (6 points – 1+2+1+1+1)

**1)** Que signifient les acronymes suivants : PID, PCB, PSW, IPC ?

**2)** Pour chacune des transitions suivantes entre les états possibles d'un processus, indiquez si la transition est possible. Si c'est le cas, donnez un exemple d'un élément qui pourrait en être à l'origine.

(a) En cours d'exécution (élu/actif) → prêt
(b) En cours d'exécution (élu/actif) → bloqué
(c) Prêt → terminé
(d) Prêt → en cours d'exécution (élu)
(e) Bloqué → en cours d'exécution (élu)

<!-- TODO: unclear in source, verify against original PDF — the answer/annotation text for this question ("autorise, dispatcher (1. pool)", "ETS / dispue", etc.) is illegible OCR of handwritten annotations; the five sub-questions (a)-(e) themselves are transcribed cleanly above from the printed statement -->

**3)** Donnez les 2 principales raisons menant à la synchronisation entre processus. Justifiez vos réponses.

**4)** Quelle est la signification du terme attente active ? Quels autres types d'attente existent-ils ? Comment peut-on éviter l'attente active ?

**5)** Comment pourrait-on utiliser un sémaphore de sorte à ce qu'un processus se bloque si un évènement n'a pas encore eu lieu, cet évènement étant provoqué par un autre processus ?

<!-- TODO: unclear in source, verify against original PDF — no legible correction/answer text was extracted for questions 3, 4, and 5 (only illegible OCR fragments like "Decofe", "sem deblocage prla Cooper Werte 2 pr") -->

## Exercice 2 : Processus Unix (4 points – 1+3)

Soit le programme suivant :

```c
main() {
    int PID, PID1, PID2;
    PID1 = fork();
    PID2 = fork();
    if (PID1 == 0) {
        PID = getpid();
        printf("A %d\n", PID);
    } else
        printf("B= %d\n", PID2);

    if (PID2 == 0) {
        PID = getpid();
        printf("C %d\n", PID);
    }
}
```

<!-- TODO: unclear in source, verify against original PDF — the exact control flow of this code (particularly around the "else" branches and the second if-block) is a best-effort reconstruction from garbled OCR fragments ("if (PID1 == 0) {... -jesus printf(\"A %d\\n\", PID); } else printf(\"B= %d\\n\",... Je Suis... else... 1et printf(d\\n\", PID2); } if (PID2==0) {...}"); the reconstruction above assumes a single if/else on PID1 followed by an unconditional if on PID2, dropping an apparently duplicated/garbled second "else" fragment that could not be placed sensibly — verify against original PDF page -->

**1.** Représentez, sous la forme d'un arbre, les processus créés par ce programme et leurs relations de parenté.

**2.** Donnez le résultat de l'exécution de ce programme, pour chaque processus, en utilisant des valeurs de PID que vous inventerez.

<!-- TODO: no legible correction was extracted for this exercise -->

## Exercice 3 : Synchronisation par sémaphores (4 points)

Soit l'ensemble de processus suivant, où les contraintes de précédence sont données par le graphe suivant :

<!-- TODO: unclear in source, verify against original PDF — the precedence graph itself (referenced as "le graphe suivant", involving processes P1, P3, P4, P5, P6 per the garbled OCR) did not extract as text/image; only fragments of a sémaphore-based skeleton solution survived ("sem S1 S2 S3=0", "P(S1) P(S1) P(S1) P(S1)... V(S1) V(S2)... V(S3)...") which are too fragmentary to reliably reconstruct as a full solution -->

Donner une solution utilisant des sémaphores (3 au plus) et permettant de synchroniser ces processus de manière à respecter les contraintes de précédence.

<!-- TODO: no legible correction was extracted for this exercise -->

</TabItem>
<TabItem value="pdf" label="PDF">

<PdfViewer file="/pdfs/se2-ds-2009-2010.pdf" />

</TabItem>
</Tabs>
